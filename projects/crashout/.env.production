# Build-time config for `vite build` (read by src/analytics/logger.ts).
# Public, keyless ingest endpoint — safe to commit (the function authorizes no
# one; it validates and inserts server-side with a key it never ships).
VITE_INSFORGE_EVENTS_URL=https://2zzc6u78.functions.insforge.app/events
