name: Crashout CI

on:
  pull_request:
    paths:
      - ".github/workflows/crashout-ci.yml"
      - "memories/consensus.md"
      - "projects/crashout/**"
  push:
    branches:
      - master
      - main
    paths:
      - ".github/workflows/crashout-ci.yml"
      - "memories/consensus.md"
      - "projects/crashout/**"

concurrency:
  group: crashout-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint, test, build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/crashout
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.5.1
          cache: true
          cache_dependency_path: projects/crashout/pnpm-lock.yaml
          package_json_file: projects/crashout/package.json

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run release gate
        run: pnpm run check

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run cockpit smoke
        run: pnpm run smoke:cockpit

      - name: Upload cockpit smoke artifacts
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: cockpit-smoke
          path: docs/qa/cockpit-smoke/
          retention-days: 14
